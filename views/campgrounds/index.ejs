<% layout('layouts/boilerplate') %>
    <div id="cluster-map" ></div>
    <h1>ALL CAMPGROUNDS</h1>
    <div><a href="/campgrounds/new">ADD NEW CAMPGROUNDS</a></div>

    <% for(let campground of campgrounds){ %>

        <div class="card mb-3">
            <div class="row">
                <div class="col-md-4">
                    <% if(campground.images.length) {%>
                        <img crossorigin="anonymous" src="<%=campground.images[0].url %>" alt="" class="img-fluid">
                        <% }else{ %>
                            <img crossorigin="anonymous" class="img-fluid"
                                src="https://images.unsplash.com/photo-1682686580922-2e594f8bdaa7?w=1000&auto=format&fit=crop&q=60&ixlib=rb-4.0.3&ixid=M3wxMjA3fDF8MHxzZWFyY2h8MXx8Y2FtcGluZ3xlbnwwfHwwfHx8MA%3D%3D"
                                alt="">
                            <% } %>
                </div>
                <div class="col-md-6">
                    <div class="card-body">
                        <h5 class="card-title">
                            <%= campground.title %>
                        </h5>
                       
                        <p class="card-text">
                            <%= campground.location %>
                        </p>
                        <p class="card-text">
                            <%= campground.description %>
                        </p>
                        <p class="card-text">
                            Price: ₹ <%= campground.price %>
                        </p>
                        <a href="/campgrounds/<%= campground._id  %>" class="btn btn-primary">View <%=campground.title
                                %>
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <% } %>
        <script>
            const mapToken = '<%-process.env.MAPBOX_TOKEN  %>'
            const campgrounds = {features:<%- JSON.stringify(campgrounds) %>};
        </script>
            <script src="/javascripts/clusterMaps.js"></script>